# 一、C++ 运算符重载参数规则速查表

## 1.单目运算符（如 ++、--、-（负号））

| 实现方式 | 形参数量 | 说明 |
|--------|---------|------|
| 成员函数 | 0 个 | 操作数由 this 指针隐式传递，如 obj.operator++() |
| 非成员函数 | 1 个 | 需显式传入操作数，如 operator++(obj) |

注意：++/-- 区分前后置时，后置版本会多一个无用的 int 占位参数，仅用于语法区分，不传递实际值。

## 2.双目运算符（如 +、-、*、/、==、&gt;）

| 实现方式 | 形参数量 | 说明 |
|--------|---------|------|
| 成员函数 | 1 个 | 左操作数由 this 指针隐式传递，右操作数作为形参传入，如 obj.operator+(other) |
| 非成员函数 | 2 个 | 左右操作数都需显式传入，如 operator+(obj1, obj2) |

## 3.特殊运算符补充
=（赋值运算符）：只能用成员函数重载，形参数量为 1 个（右操作数）。

[]（下标运算符）：只能用成员函数重载，形参数量为 1 个（下标值）。

()（函数调用运算符）：只能用成员函数重载，形参数量可自定义。

# 二、赋值运算符重载
## 1.同类对象互相赋值
```cpp
类名& operator=(const 类名& other)
{
    // 1. 防自赋值判断（核心第一句）
    if (this == &other)
        return *this;

    // 2. 释放当前对象自己堆上的资源（如果有动态内存才写）
    // delete 自己的数组/指针等

    // 3. 拷贝other的全部数据（浅拷贝/深拷贝）
    成员1 = other.成员1;
    成员2 = other.成员2;

    // 4. 返回*this，支持链式赋值 a=b=c;
    return *this;
}
```
```cpp
场景：类里有 char* 字符串指针
StrClass& StrClass::operator=(const StrClass& rhs)
{
    // 1. 检查自赋值：自己给自己赋值，直接退出
    if(this == &rhs)
        return *this;

    // 2. 释放当前对象原有堆内存
    delete[] str;

    // 3. 分配新内存 + 拷贝数据
    length = strlen(rhs.str);
    str = new char[length + 1];
    strcpy(str, rhs.str);

    // 4. 返回自身引用，支持连续赋值
    return *this;
}
```
## 2.类对象接收基础类型
```cpp
Circle& operator=(double r)
{
    radius = r;
    return *this;
}
```

# 三、输入输出运算符重载
```c
public:
    // 友元声明
    friend istream& operator>>(istream& is, 类名& 对象);
    friend ostream& operator<<(ostream& os, const 类名& 对象);
};

// 类外实现
istream& operator>>(ostream& is, const 类名& 对象)
{
    is << ...; // 输入内容
    return is;
}
ostream& operator<<(ostream& os, const 类名& 对象)
{
    os << ...; // 输出内容
    return os;
}

//运算符重载里面的形参（对象）叫什么无所谓,这里习惯用obj
```


# 四、类型转换重载
```cpp
operator 目标类型() const //没有返回值类型，没有参数，通常声明为const
{
    // 转换逻辑，return 对应类型的值
} 
```


# 五、字符数组 & 指针
## 1.定长字符串 → 直接用 char 数组，不用指针；
## 2.变长 / 动态字符串 → 用 char* + new[] → 必写：析构、拷贝构造、赋值重载（深拷贝三大函数）
```cpp
class Str
{
private:
    char* str;  // 指针成员，指向堆内存
public:
    // 构造函数：在堆上开辟内存
    Str(const char* s)
    {
        int len = strlen(s);
        str = new char[len + 1]; // 动态申请堆内存
        strcpy(str, s);
    }
    // 析构函数：释放堆内存
    ~Str()
    {
        delete[] str; 
    }
};
```
### （1）拷贝构造函数：新建对象，无旧内存要释放，只分配新内存
```cpp
SimpleCircle c3(c2);        // 直接调用拷贝构造
SimpleCircle c4 = c2;       // 等价拷贝构造
func(SimpleCircle c);       // 值传递传参，临时对象构造
return SimpleCircle(c2);    // 函数返回值生成临时对象
```
###  (2)赋值运算符重载：两个已存在对象，先释放自身旧内存，再复制资源
```cpp
SimpleCircle a(5), b(10);
a = b;  // 赋值重载，a、b都已经存在
```
## 3.指针不 new、只借用外部地址 → 浅拷贝即可，不用额外处理。

# 六、类名后加 & 引用的三种核心场景
## 1.形参优先加 const &：只读不拷贝，运算符重载标配
`Vector2D operator-(const Vector2D& other) const`
## 2.前置 ++、--、+=、-=、= 必须返回引用&&普通双目 +、-、后置 ++ 、产生新临时对象的不能返回引用
```cpp
Vector2D& operator++()
{
    x++;y++;
    return *this; // 返回自身引用
}
```
## 3.给已有对象起别名，共用同一块内存
```cpp
Vector2D v1(1,2);
Vector2D& v2 = v1; // v2是v1的别名，改v2就是改v1
```

# 七、virtual
## 1.虚函数与虚继承
| 使用位置 | 语法形式 | 核心作用 | 解决的问题 |
|--------|---------|------|---------|
| 继承修饰	 | virtual public 基类 | 虚继承，共享唯一基类副本 | 多重继承二义性、重复存储基类 |
| 函数修饰 | virtual 返回值 函数名() | 虚函数，支持运行时多态、函数覆盖 | 实现多态，子类重写函数动态调用 |
## 2.虚函数与纯虚函数
| 形式 | 写法标记 | 基类能否创建对象 | 子类是否必须重写 |
|--------|---------|------|---------|
| 普通虚函数 | virtual 函数(){ 实现 } | ✅ 可以创建 People 对象 | 可选重写，不重写沿用父类 |
| 纯虚函数（抽象类） | virtual 函数() = 0; | ❌ 禁止创建 People 对象 | 必须全部重写 |

