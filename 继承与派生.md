# 显式基类构造调用
## 调用规则：派生类构造函数初始化列表中可以选择调用基类的任意可用构造函数；若无明确调用，则调用基类默认构造函数。
## 意义：确保基类部分先于派生类部分正确初始化，维护继承层次的对象完整性，并允许将参数传递给基类。
```cpp
CDate(int y, int m, int d, int h, int mi, int s) : CTime(h, mi, s) {
        year = y; month = m; day = d;
}
```

# 数据成员的调用
## 1.基类私有变量 + 继承调用公有getXX () 
```cpp
class Base {
private:
    int a;  // 私有
public:
    int getA() { return a; } // 公开接口
};
```
## 2.基类保护变量 + 公有继承，派生类直接调用
## 3.友元函数直接访问类的 private/protected
```cpp
class 类名 {
private:
    int x;  // 私有变量
public:
    // 声明友元函数
    friend void 函数名(类名 &obj);
};
void 函数名(类名 &obj) {
    // 这里可以直接访问 obj.x（私有变量）
}
```
# 三种成员属性在三种继承方式下的访问权限变化表
| 基类成员属性 | 公有继承（public） | 保护继承（protected） | 私有继承（private） |
|--------|---------|------|---------|
| private | 派生类中不可直接访问 | 派生类中不可直接访问 | 派生类中不可直接访问 |
| public | public，外部可访问 | protected，外部不可访问 | private，外部不可访问 |
| protected | protected，外部不可访问 | protected，外部不可访问 | private，外部不可访问 |
