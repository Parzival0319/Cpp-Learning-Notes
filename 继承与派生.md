# 显式基类构造调用
## 调用规则：派生类构造函数初始化列表中可以选择调用基类的任意可用构造函数；若无明确调用，则调用基类默认构造函数。
## 意义：确保基类部分先于派生类部分正确初始化，维护继承层次的对象完整性，并允许将参数传递给基类。
```cpp
CDate(int y, int m, int d, int h, int mi, int s) : CTime(h, mi, s) {
        year = y; month = m; day = d;
}
```
