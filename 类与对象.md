# 构造函数
## 只有简单赋值、不用互相计算：用 : radius(r) 初始化列表
` Circle(double r=0) : radius(r) {} `
## 成员变量需要先后计算、互相依赖：就写在函数体内赋值
```cpp
 Circle(double r = 0)
{
    radius = r;
    perimeter = 2*PI*radius;
    area = PI*radius*radius;
}
```
