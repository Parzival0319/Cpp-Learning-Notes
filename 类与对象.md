# 一、构造函数
## 只有简单赋值、不用互相计算：用初始化列表
` Circle(double r=0) : radius(r) {} `
## 成员变量需要先后计算、互相依赖：就写在函数体内赋值
```cpp
 Circle(double r = 0)
{
    radius = r;
    perimeter = 2*PI*radius;
    area = PI*radius*radius;
}
```
##  只保留一个带默认参数的构造，解决二义性（同时满足默认构造和参数构造）
`Time(int h = 1, int m = 2, int s = 3) : hour(h), minute(m), second(s) {}`

## 拷贝构造
`Time(const Time& other) : hour(other.hour), minute(other.minute), second(other.second) {}`
# 二、类成员函数类外实现
## 返回值类型 类名::函数名(参数列表) { 函数体 }

# 三、创建对象
## 方式 1：单个局部对象（栈上）
`Fraction a;`
## 方式 2：动态数组里的对象（堆上）
```cpp
int n; cin >> n; 
Fraction *f = new Fraction[n];
//在堆上动态创建一个长度为 n 的 Fraction 对象数组，(循环执行 n 次 Fraction() 无参构造函数，创建 n 个独立的 Fraction 对象)
//并用指针变量 f(类型是 Fraction*,只能存储 Fraction 类对象的地址) 指向这个数组的第一个元素。
//后续可以像普通数组一样用 f[0]、f[1]...f[n-1] 访问每个分数对象。
```
