# 移动操作
##  非模板普通函数：参数是值类型`T e`，函数体内放心`std::move(e)`；e 是局部，外面看不见，掏空没关系。
## 模板包装函数，接收`U&&`转发引用：**绝对不要 std::move，用 forward**。
## 普通`T&&`右值引用（非模板）：调用端必须手动 move 传入；函数体内再 move 一次使用，调用者清楚知道对象会被掏空。
形参类型	是否需要在函数内部 std::move	说明
const T&	❌ 绝对不要	const 对象不能被移动
T&	⚠️尽量不要	会掠夺外部实参资源，可读性差
T&&（非模板）	✅ 需要	形参名字是左值，必须 move 才触发移动
template<U> U&&	❌用 forward，不用 move	完美转发，保留原始值类别
T 值参数	✅建议 move	局部对象，move 无副作用
