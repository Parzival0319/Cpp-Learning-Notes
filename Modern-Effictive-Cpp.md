# 移动操作
|形参类型|	是否需要在函数内部std::move| 说明|
|------|--------|-------|
|const T&|	❌ 绝对不要|	const 对象不能被移动 |
|T&|	⚠️尽量不要|	会掠夺外部实参资源，可读性差 |
|T&&（非模板）|	✅ 需要|	形参名字是左值，必须 move 才触发移动|
|template<U> U&&|	❌用 forward，不用 move	|完美转发，保留原始值类别|
|T 值参数|	✅建议| move	局部对象，move 无副作用|
## 1. 如果只是读取：用`const T&`，不要 move。
## 2. 如果打算接管资源：
   - 简单方案：用**值参数 T**，函数内部`std::move`；调用方可以传`std::move(obj)`来避免拷贝。
   - 高性能模板场景：用**转发引用`U&&` + `std::forward<U>`**。
## 3. 尽量少写裸的`T&&`非模板右值引用，可读性差，调用方必须手动`std::move`才能匹配。
## 4. 千万不要接收普通左值引用`T&`，然后内部偷偷 move，会造成外部对象意外被掏空，极难调试。
